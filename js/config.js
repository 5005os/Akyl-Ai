/* ============ AI KYRGYZSTAN — конфигурация ============
 *
 * Заполни эти значения, чтобы включить ВХОД ПО ПОЧТЕ.
 * Пока значения пустые — сайт работает в открытом режиме (без входа).
 *
 * SUPABASE_URL и SUPABASE_ANON_KEY — бесплатно на https://supabase.com
 * (Project Settings → API). anon key публичный, его можно показывать.
 *
 * ВАЖНО: сюда НЕЛЬЗЯ вписывать ключи ИИ (Gemini/Claude и т.п.) — это статический
 * сайт, весь js/ виден любому посетителю через «просмотр кода страницы», и ключ
 * тут же утечёт и будет использован не по назначению. Режим «Бесплатно» (Pollinations)
 * ключа не требует; для Gemini/Claude каждый пользователь вводит свой ключ в ⚙️
 * настройках — он хранится только в его браузере (localStorage).
 */
window.AIKG_CONFIG = {
  SUPABASE_URL: "",       // ← вставь Project URL
  SUPABASE_ANON_KEY: ""   // ← вставь anon (public) key
};
